# Kondoo Bot Configuration Template
# -----------------------------------------------------------------------------
# INSTRUCTIONS:
# 1. Rename this file to ".env" in your specific bot configuration directory
#    (e.g., 'kondoo/mavi-config/.env').
# 2. Fill in the required values below. Do NOT commit the ".env" file to Git.
# -----------------------------------------------------------------------------

# =============================================================================
#  PROVIDER SELECTION
#  Choose the services for core functionalities.
# =============================================================================

# -- Answer Engine Provider --
#  Determines the LLM service that will generate answers.
#  - Cloud Options: "gemini", "openai", "deepseek"
#  - Self-Hosted Options: "ollama_compatible"
ANSWER_LLM_PROVIDER=gemini

# -- Knowledge Embedding Provider --
#  Determines the service used to vectorize documents.
#  - Cloud Options: "openai"
#  - Self-Hosted Options: "ollama"
#  - Local-Only Options: "local" (runs on CPU/GPU without a server)
KNOWLEDGE_PROVIDER=ollama


# =============================================================================
#  ANSWER ENGINE CONFIGURATION (Settings for your chosen ANSWER_LLM_PROVIDER)
# =============================================================================

# --- For Cloud API Providers (gemini, openai, deepseek) ---
# Your secret API key for the chosen cloud service.
LLM_API_KEY=""
# The specific model name you want to use.
# Examples: "models/gemini-1.5-flash", "gpt-4o", "deepseek-chat"
LLM_MODEL_NAME="models/gemini-1.5-flash"


# --- For Self-Hosted Providers (ollama_compatible) ---
# The base URL of your self-hosted LLM service.
# This is ignored by Cloud API Providers.
LLM_BASE_URL="http://localhost:11434"
# Some self-hosted endpoints might require an API key, others not.
# LLM_API_KEY="ollama" # Often not required, but available if needed.


# =============================================================================
#  BOT & KNOWLEDGE BASE CONFIGURATION
# =============================================================================

# -- Path to the bot's personality file --
# This file contains the main prompt that defines the bot's behavior.
# The path is relative to the container's /app directory.
BOT_PERSONALITY_FILE=/app/personality.txt

# -- Path to the knowledge base directory --
# Internal path inside the container where the knowledge base is mounted.
KNOWLEDGE_DIR=/app/knowledge